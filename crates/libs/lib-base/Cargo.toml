[package]
name = "lib-base"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[lints]
workspace = true

[dependencies]
base64 = "0.21"
serde_json = "1"
time = {version = "0.3", features = ["formatting", "parsing", "serde"]}
